{% extends 'base.html.twig' %}

{% block title %}Modifier un Gif{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Modifier le Gif</h1>

                {{ include('gif/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                <br>
                <a href="{{ path('app_gif_index') }}" class="btn btn-dark btn-block">Retour à la liste</a>
                <div class="d-flex mt-3">
                    {{ include('gif/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
