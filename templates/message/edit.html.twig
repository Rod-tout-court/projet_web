{% extends 'base.html.twig' %}

{% block title %}Modifier le message{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Modifier un message</h1>
            
                <div class="d-flex flex-column align-items-center">
                    {{ include('message/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    <a class="btn btn-dark mt-3" href="{{ path('app_message_index') }}">Retour</a>
                    {{ include('message/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
